<!DOCTYPE html>
<html>
  <head>
    <title>Edit</title>
  </head>

  <div class="container">
    <% if @gossip.errors.any? %>
      <div class="alert alert-danger" role="alert">
        Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :
        <ul>
          <% @gossip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
          <% end %>
        </ul>
      </div>
      <% elsif false %>
      <div class="alert alert-success" role="alert">
        Votre gossip a bien été modifié ! 
      </div>
    <% end %>

    <div class="row">
      <h1>Editer un gossip !</h1>
    </div>
    
    <div class="row justify-content-center">
      <%= form_for @gossip do |f| %>
      <div class="form-group col-12">
        <label>Titre du gossip</label>
        <%= f.text_field :title, class: "form-control"%>
      </div>
      <div class="form-group col-12">
        <label>Contenu du gossip</label>
        <%= f.text_field :content, class: "form-control"%>
      </div>
      <div class="form-group text-right col-12">
        <button type="submit" class="btn btn-warning">Modifier le gossip</button>
      </div>
      <% end %>
    </div>
  </div>
</html>